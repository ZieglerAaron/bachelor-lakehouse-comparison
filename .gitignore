# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
logs/

# Temporary files
*.tmp
*.temp

# Docker volumes (wenn sie als Bind Mounts verwendet werden)
# data/
# volumes/

# JAR files (werden dynamisch heruntergeladen)
*.jar

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
env.bak/
venv.bak/

# Spark
spark-events/
spark-logs/

# MinIO data (wird in Docker volumes gespeichert)
minio-data/

# PostgreSQL data (wird in Docker volumes gespeichert)
postgres-data/ 

# Airflow
airflow/logs/
airflow/__pycache__/ 

# MinIO Systemverzeichnisse und temporäre/verarbeitete Daten ignorieren
/delta/test-data/.minio.sys/
/delta/test-data/buckets/
/delta/test-data/wba/
/delta/test-data/tmp/
/delta/test-data/format.json

# Die Originaldatei von Docker Compose (docker-compose.yml.original) wird ignoriert,
# da sie als Vorlage dient und nicht verändert oder versioniert werden soll.
delta/docker-compose.yml.original

metastore_db/
warehouse/
derby.log
Tests/B/

# Delta Lake Output
delta/spark-apps/delta-warehouse/
delta/spark-apps/delta-tables/
delta/spark-apps/delta-output/
delta/spark-apps/parquet/

# Spark Job Output
**/_SUCCESS
**/part-*
**/.part-*

# Iceberg Output (falls vorhanden)
iceberg/warehouse/
**/metadata/
**/snap-*.avro

# Allgemeine Output-Verzeichnisse
**/output/
**/data/
**/*.parquet
**/*.orc